const { ROLES } = require('./constants/roles');
const { REPORT_STATUS, PRIORITY_LEVEL } = require('./constants/reportStatus');
const { WASTE_CATEGORIES } = require('./constants/wasteCategories');

module.exports = { ROLES, REPORT_STATUS, PRIORITY_LEVEL, WASTE_CATEGORIES };
